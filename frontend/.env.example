# The API origin the Vite dev proxy forwards /api and /internal to.
# Production is same-site (Vercel rewrites → Render), so this is a dev-only value.
VITE_API_BASE_URL=http://localhost:8000

# Serve the app against MSW fixtures instead of a real backend.
# VITE_ENABLE_MOCKS=true
