# `vite build --mode mock` picks this up. Used for the Lighthouse run and for design work with no
# backend running — Vite loads it by mode, so no cross-env dependency is needed.
VITE_ENABLE_MOCKS=true
